[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && npx prisma migrate deploy && npx prisma generate && node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[service]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
